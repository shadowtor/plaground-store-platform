---
phase: 01-plaground-commerce
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - apps/api/src/plugins/auth/
  - apps/api/src/plugins/rbac/
  - apps/api/src/routes/v1/auth/
  - apps/api/src/routes/v1/admin/
  - apps/api/src/services/auth/
  - apps/api/src/services/mfa/
  - apps/api/src/services/session/
  - apps/api/src/services/audit/
  - apps/web-storefront/app/(auth)/
  - apps/web-admin/app/(auth)/
  - packages/contracts/src/
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - ADMIN-08
user_setup:
  - Configure Discord OAuth app credentials before exercising the Discord login/link flow end to end.
must_haves:
  truths:
    - Customers can register, log in, log out, and request password resets through typed API routes and real web forms.
    - Admin sessions enforce MFA and shorter timeout rules before any privileged admin route succeeds.
    - RBAC is deny-by-default for Guest, Customer, Staff, Admin, and ConnectorNode permissions.
  artifacts:
    - apps/api/src/routes/v1/auth/
    - apps/api/src/plugins/rbac/
    - apps/api/src/services/mfa/
    - apps/web-storefront/app/(auth)/
    - apps/web-admin/app/(auth)/
  key_links:
    - Web auth forms use contracts/types emitted by the shared contracts package rather than API source imports.
    - Admin MFA and RBAC checks write to audit logging hooks for privileged events and security changes.
    - Session lifetime rules differ between storefront/customer and admin contexts.
---

<objective>
Implement the identity, session, and permission model that all protected customer and admin flows depend on.

Purpose: Secure the platform before commerce features go live and ensure admin access is gated by MFA plus explicit permissions.
Output: Auth routes/services, session and RBAC plugins, customer/admin auth screens, and the permission foundation for later plans.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plaground-commerce/01-CONTEXT.md
@.planning/phases/01-plaground-commerce/01-RESEARCH.md
@.planning/phases/01-plaground-commerce/01-VALIDATION.md
@.planning/phases/01-plaground-commerce/01-01-PLAN.md
@specs/001-platform-foundation/spec.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement API-side customer auth and session flows</name>
  <files>apps/api/src/routes/v1/auth/, apps/api/src/services/auth/, apps/api/src/services/session/, packages/contracts/src/</files>
  <read_first>.planning/phases/01-plaground-commerce/01-01-PLAN.md, .planning/phases/01-plaground-commerce/01-VALIDATION.md, specs/001-platform-foundation/spec.md, AGENTS.md</read_first>
  <action>Create typed API endpoints for register, login, logout, session refresh or session lookup, email verification, password reset request, and password reset completion. Use Argon2 for password hashing, httpOnly Secure cookies for sessions, CSRF protection on unsafe methods, and clear error envelopes. Add contract exports for the auth request/response shapes so the web apps consume generated/shared types instead of importing API internals.</action>
  <verify>Read the auth route and service files for each named flow and check the contracts package exposes matching shapes.</verify>
  <acceptance_criteria>
    - apps/api/src/routes/v1/auth/ contains handlers for register, login, logout, password reset request, and password reset completion
    - apps/api/src/services/auth/ contains the string "argon2"
    - auth route code contains "httpOnly" and "secure" cookie settings
    - packages/contracts/src contains exported auth schema or generated type names for register and login
  </acceptance_criteria>
  <done>Customer-facing auth flows exist in the API and are typed for both web apps.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce admin MFA, role checks, and session lifetime separation</name>
  <files>apps/api/src/plugins/auth/, apps/api/src/plugins/rbac/, apps/api/src/services/mfa/, apps/api/src/routes/v1/admin/, apps/api/src/services/audit/</files>
  <read_first>.planning/phases/01-plaground-commerce/01-CONTEXT.md, .planning/phases/01-plaground-commerce/01-RESEARCH.md, apps/api/src/lib/prisma.ts, apps/api/src/plugins/audit-log.ts</read_first>
  <action>Implement deny-by-default RBAC middleware around named permissions and map the five roles Guest, Customer, Staff, Admin, and ConnectorNode. Add admin MFA enrollment and challenge flows using TOTP or passkey-ready abstractions, then require a successful MFA challenge before privileged admin routes continue. Encode shorter admin session lifetimes and inactivity timeouts than customer sessions, and record security-sensitive state changes such as MFA enrollment/reset in the audit service.</action>
  <verify>Read the auth and RBAC plugins plus admin auth routes to confirm role gates, MFA checks, and shorter admin session settings are explicitly defined.</verify>
  <acceptance_criteria>
    - apps/api/src/plugins/rbac/ contains the exact role names "Guest", "Customer", "Staff", "Admin", and "ConnectorNode"
    - apps/api/src/services/mfa/ contains "totp" or "@otplib"
    - admin auth or auth plugin code contains a branch that blocks admin login until MFA succeeds
    - admin session configuration contains a shorter max age or inactivity timeout than the customer session configuration
  </acceptance_criteria>
  <done>Admin authentication is materially stronger than customer authentication and RBAC is explicit.</done>
</task>

<task type="auto">
  <name>Task 3: Build the storefront and admin auth entry surfaces</name>
  <files>apps/web-storefront/app/(auth)/, apps/web-admin/app/(auth)/</files>
  <read_first>.planning/phases/01-plaground-commerce/01-UI-SPEC.md, .planning/phases/01-plaground-commerce/01-01-PLAN.md, apps/web-storefront/package.json, apps/web-admin/package.json</read_first>
  <action>Create customer registration/login/reset pages in the storefront app and a separate admin login plus MFA challenge flow in the admin app. Use the shared token system and typography/colors from 01-UI-SPEC.md, keep forms on the generated/shared contract shapes, and include loading, success, empty, and error states for each auth mutation. Keep the admin auth shell dark-first and visually distinct from the public storefront shell.</action>
  <verify>Read the new route files and confirm each auth surface includes form, loading, and error handling states.</verify>
  <acceptance_criteria>
    - apps/web-storefront/app/(auth)/ contains pages for login, register, and reset password
    - apps/web-admin/app/(auth)/ contains pages for login and MFA verification or setup
    - auth page files contain user-facing error copy and loading-state handling
    - admin auth pages use dark-first layout tokens or classes consistent with 01-UI-SPEC.md
  </acceptance_criteria>
  <done>Both web apps expose the auth flows needed for the rest of Phase 1.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Read the API auth routes and confirm register, login, logout, password reset, and verification flows exist.
- [ ] Read the RBAC and MFA code paths and confirm deny-by-default checks plus admin MFA enforcement are present.
- [ ] Read the new storefront and admin auth pages and confirm loading/error states exist for each surface.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 has reusable auth, session, and RBAC primitives for every protected route
  </success_criteria>

<output>
After completion, create `.planning/phases/01-plaground-commerce/01-02-SUMMARY.md`
</output>
