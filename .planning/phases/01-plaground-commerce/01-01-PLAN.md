---
phase: 01-plaground-commerce
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - tsconfig.base.json
  - .env.example
  - infra/compose/docker-compose.yml
  - infra/compose/docker-compose.override.yml
  - apps/api/package.json
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations/
  - apps/api/src/lib/
  - apps/api/src/plugins/
  - apps/api/src/routes/
  - apps/worker/package.json
  - apps/web-storefront/package.json
  - apps/web-admin/package.json
  - apps/connector/package.json
  - packages/contracts/package.json
  - packages/config/package.json
  - packages/ui/package.json
  - vitest.config.ts
  - playwright.config.ts
autonomous: true
requirements:
  - Infrastructure
user_setup: []
must_haves:
  truths:
    - A fresh clone can install workspace dependencies and start the full Docker dev stack without hidden host-only services.
    - Every tenant-scoped Phase 1 table is created with tenant-aware schema, RLS policy scaffolding, and tests that run as app_user rather than the migration superuser.
    - Shared workspace config, contracts generation, and validation/test scaffolding exist before feature plans begin.
  artifacts:
    - pnpm-workspace.yaml
    - turbo.json
    - infra/compose/docker-compose.yml
    - apps/api/prisma/schema.prisma
    - vitest.config.ts
    - playwright.config.ts
  key_links:
    - Docker compose wires postgres, pgbouncer, redis, object storage emulator, mail catcher, api, worker, storefront, and admin together.
    - Prisma schema, migrations, and RLS helpers align with the tenant isolation decisions in 01-CONTEXT.md and 01-VALIDATION.md.
    - packages/contracts and packages/config are consumable by apps/api, apps/web-storefront, apps/web-admin, apps/worker, and apps/connector.
---

<objective>
Establish the executable monorepo foundation for Phase 1 before any product features are built.

Purpose: Make the project runnable, type-safe, Docker-first, and tenant-aware from the first commit so later plans do not need to retrofit infrastructure.
Output: Workspace tooling, Docker compose stack, Prisma schema/migrations with RLS scaffolding, shared package foundations, and baseline validation/test config.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plaground-commerce/01-CONTEXT.md
@.planning/phases/01-plaground-commerce/01-RESEARCH.md
@.planning/phases/01-plaground-commerce/01-VALIDATION.md
@specs/001-platform-foundation/spec.md
@specs/001-platform-foundation/plan.md
@specs/001-platform-foundation/data-model.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the workspace and shared package boundaries</name>
  <files>package.json, pnpm-workspace.yaml, turbo.json, tsconfig.base.json, packages/contracts/package.json, packages/config/package.json, packages/ui/package.json, apps/api/package.json, apps/worker/package.json, apps/web-storefront/package.json, apps/web-admin/package.json, apps/connector/package.json</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-plaground-commerce/01-CONTEXT.md, .planning/phases/01-plaground-commerce/01-RESEARCH.md, AGENTS.md</read_first>
  <action>Create the monorepo root manifests for pnpm workspaces plus Turborepo. Define workspaces for apps/api, apps/worker, apps/web-storefront, apps/web-admin, apps/connector, packages/contracts, packages/config, and packages/ui. Add root scripts for dev, build, lint, typecheck, test, db:migrate, db:seed, contracts:generate, and docker helper flows. Configure shared strict TypeScript defaults in tsconfig.base.json and ensure each package manifest points at its own build, typecheck, and test entrypoints instead of cross-importing app source directly.</action>
  <verify>Read the created manifests and confirm each workspace path and script exists exactly once.</verify>
  <acceptance_criteria>
    - pnpm-workspace.yaml contains "apps/*" and "packages/*"
    - turbo.json contains tasks for "build", "typecheck", "lint", and "test"
    - package.json contains scripts named "dev", "build", "typecheck", "lint", "test", "db:migrate", and "contracts:generate"
    - apps/api/package.json contains dependencies for Fastify, Prisma, Zod, and BullMQ
  </acceptance_criteria>
  <done>Workspace manifests exist and show the agreed monorepo topology with strict TypeScript tooling.</done>
</task>

<task type="auto">
  <name>Task 2: Stand up the Docker-first local environment and service wiring</name>
  <files>.env.example, infra/compose/docker-compose.yml, infra/compose/docker-compose.override.yml</files>
  <read_first>.planning/phases/01-plaground-commerce/01-CONTEXT.md, .planning/phases/01-plaground-commerce/01-RESEARCH.md, specs/001-platform-foundation/plan.md, AGENTS.md</read_first>
  <action>Create the compose stack for postgres, pgbouncer, redis, object storage emulator, mail catcher, api, worker, web-storefront, and web-admin with bind mounts and health checks. Define environment placeholders in .env.example for database, redis, object storage, Stripe, PayPal, Discord, session, and email configuration. Keep the stack Docker-first: no host-installed Postgres, Redis, or Node assumptions. Add ports for the two web apps, API, Mailpit UI, and object storage console only. Include startup ordering so api and worker wait on postgres, pgbouncer, and redis.</action>
  <verify>Inspect compose YAML and .env.example for the required services, env keys, and health checks.</verify>
  <acceptance_criteria>
    - infra/compose/docker-compose.yml contains services named "postgres", "pgbouncer", "redis", "api", "worker", "web-storefront", and "web-admin"
    - infra/compose/docker-compose.yml contains either "minio" or another S3 emulator service and "mailpit"
    - .env.example contains "DATABASE_URL=", "DIRECT_DATABASE_URL=", "REDIS_URL=", "STRIPE_SECRET_KEY=", and "PAYPAL_CLIENT_ID="
    - compose files include bind-mounted source volumes for api and both web apps
  </acceptance_criteria>
  <done>The repository documents and encodes a one-command Docker development environment.</done>
</task>

<task type="auto">
  <name>Task 3: Define the full Prisma schema, migration baseline, and RLS enforcement scaffolding</name>
  <files>apps/api/prisma/schema.prisma, apps/api/prisma/migrations/, apps/api/src/lib/prisma.ts, apps/api/src/lib/prisma-rls.ts, apps/api/src/lib/prisma-rls.test.ts, apps/api/src/plugins/audit-log.ts</files>
  <read_first>.planning/phases/01-plaground-commerce/01-CONTEXT.md, .planning/phases/01-plaground-commerce/01-RESEARCH.md, .planning/phases/01-plaground-commerce/01-VALIDATION.md, specs/001-platform-foundation/data-model.md, specs/001-platform-foundation/spec.md</read_first>
  <action>Author the initial Prisma schema with all Phase 1 core entities, PostgreSQL enums, foreign keys, and tenant_id UUID NOT NULL on every tenant-scoped table. Add the migration baseline plus SQL needed to create the app_user role, RLS policies, and append-only audit protections. Implement Prisma helpers that set SET LOCAL app.current_tenant_id inside transactions and add a focused Vitest integration test proving app_user cannot read another tenant's rows. Create the audit log plugin/service scaffold that later plans will call for privileged actions.</action>
  <verify>Read the Prisma schema and RLS test files and confirm the full-entity baseline plus app_user enforcement are present.</verify>
  <acceptance_criteria>
    - apps/api/prisma/schema.prisma contains models named "User", "Quote", "Order", "Payment", "ConnectorNode", "Printer", "PrintJob", and "AuditLogEntry"
    - apps/api/prisma/schema.prisma contains the exact string "tenantId" in each tenant-scoped model definition
    - apps/api/src/lib/prisma-rls.test.ts contains the string "app_user"
    - apps/api/src/lib/prisma-rls.ts contains the exact SQL fragment "SET LOCAL app.current_tenant_id"
  </acceptance_criteria>
  <done>The data layer is ready for downstream feature plans without later tenant retrofits.</done>
</task>

<task type="auto">
  <name>Task 4: Add shared config, contracts, and validation/test baseline files</name>
  <files>packages/config/src/, packages/contracts/src/, vitest.config.ts, playwright.config.ts, apps/api/vitest.config.ts, apps/web-storefront/e2e/checkout.spec.ts, apps/web-storefront/e2e/quote-flow.spec.ts, apps/web-admin/e2e/admin-approve.spec.ts, apps/api/src/services/auth.test.ts, apps/api/src/plugins/rbac.test.ts, apps/api/src/routes/webhooks-stripe.test.ts</files>
  <read_first>.planning/phases/01-plaground-commerce/01-VALIDATION.md, .planning/phases/01-plaground-commerce/01-RESEARCH.md, AGENTS.md</read_first>
  <action>Create the Zod-based env validation entrypoints in packages/config, the contracts package scaffold for generated OpenAPI types plus connector message schemas, and the validation baseline called for in 01-VALIDATION.md. Add root Vitest and Playwright config, API-specific Vitest config, and stub tests/files for auth, RBAC, Stripe webhook verification, storefront checkout E2E, quote-flow E2E, and admin approval E2E. Keep all files non-watch-mode and wired to root scripts added in Task 1.</action>
  <verify>Inspect the new config and test scaffold files for the exact filenames and commands required by 01-VALIDATION.md.</verify>
  <acceptance_criteria>
    - vitest.config.ts exists and references workspace projects or test roots
    - playwright.config.ts exists at the repository root
    - apps/api/vitest.config.ts exists
    - apps/web-storefront/e2e/checkout.spec.ts, apps/web-storefront/e2e/quote-flow.spec.ts, and apps/web-admin/e2e/admin-approve.spec.ts exist
    - apps/api/src/services/auth.test.ts, apps/api/src/plugins/rbac.test.ts, and apps/api/src/routes/webhooks-stripe.test.ts exist
  </acceptance_criteria>
  <done>Phase 1 has the minimum test and shared-package scaffolding required for safe feature work.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Read package.json, pnpm-workspace.yaml, turbo.json, and tsconfig.base.json to confirm workspace bootstrap is present.
- [ ] Read infra/compose/docker-compose.yml and .env.example to confirm required services and env keys are defined.
- [ ] Read apps/api/prisma/schema.prisma and apps/api/src/lib/prisma-rls.test.ts to confirm tenant-aware schema and app_user coverage.
- [ ] Read vitest.config.ts, playwright.config.ts, and the Wave 0 stub test files listed in 01-VALIDATION.md.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo can proceed to feature implementation without re-planning core infrastructure
  </success_criteria>

<output>
After completion, create `.planning/phases/01-plaground-commerce/01-01-SUMMARY.md`
</output>
