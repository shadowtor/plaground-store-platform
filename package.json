{
  "name": "plaground-platform",
  "version": "0.0.1",
  "private": true,
  "description": "PLAground 3D printing commerce and operations platform",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "db:migrate": "pnpm --filter api exec prisma migrate deploy",
    "db:migrate:dev": "pnpm --filter api exec prisma migrate dev",
    "db:seed": "pnpm --filter api exec prisma db seed",
    "db:reset": "pnpm --filter api exec prisma migrate reset",
    "db:studio": "pnpm --filter api exec prisma studio",
    "contracts:generate": "turbo run contracts:generate --filter=contracts",
    "docker:up": "docker compose -f infra/compose/docker-compose.yml -f infra/compose/docker-compose.override.yml up -d",
    "docker:down": "docker compose -f infra/compose/docker-compose.yml -f infra/compose/docker-compose.override.yml down",
    "docker:reset": "docker compose -f infra/compose/docker-compose.yml -f infra/compose/docker-compose.override.yml down -v && pnpm docker:up",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "turbo": "^2.5.3",
    "typescript": "^5.8.3",
    "prettier": "^3.5.3",
    "@types/node": "^22.14.0"
  }
}
